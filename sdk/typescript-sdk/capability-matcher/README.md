# Capability matcher moved

Capability matching utilities are located in [`packages/mew/src/capability-matcher`](../../packages/mew/src/capability-matcher).
Import them via `@mew-protocol/mew/capability-matcher`.
