# Participant package moved

Participant runtime sources are consolidated in [`packages/mew/src/participant`](../../packages/mew/src/participant).
Depend on `@mew-protocol/mew` to consume the runtime APIs.
